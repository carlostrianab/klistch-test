{% assign fetchpriority = 'auto' %}
{% assign loading = loading |  default: 'lazy' %}

{% if position < 6 or loading == 'eager' %}
    {% assign fetchpriority = 'high' %}
    {% assign loading = 'eager' %}
{% endif %}

{% assign alt = image.alt %}

{% if image.alt == blank %}
    {% assign alt = 'Kitsch image' %}
{% endif %}

{{ image  | image_url: width: 700 | image_tag: alt: alt, fetchpriority: fetchpriority, loading: loading, class: class }}
